[workspace]
changelog_config = "cliff.toml"
# Disable crates.io publishing until we're ready to release publicly.
# Remove this line when CARGO_REGISTRY_TOKEN is configured and crates are publish-ready.
publish = false
